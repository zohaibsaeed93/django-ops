# Control plane

This directory is reserved for the Django + Django REST Framework modular monolith introduced in Phase 0. Product state, deployment decisions, and Django-aware diagnostics will live here as the source of truth.

No persistent deploy-agent implementation belongs in this directory.
